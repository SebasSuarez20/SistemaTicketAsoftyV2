export interface ITicketSupport {
  idControl: number | null;
  consecutive: number | null;
  title: string;
  aerea: string;
  description: string;
  status: string;
  priority: string;
  photoDescription: string
  assigned_To: number
  enabled: boolean
}