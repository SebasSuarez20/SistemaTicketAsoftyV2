import { AccessRolUserDirective } from './access-rol-user.directive';

describe('AccessRolUserDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessRolUserDirective();
    expect(directive).toBeTruthy();
  });
});
